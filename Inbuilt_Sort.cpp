#include <bits/stdc++.h>
using namespace std;
int main()
{
    // sort()  is function to sort
    // it is combination of queck sort, heap sort, insersion sort,
    // this sort also called as introsort
    // it takes two argument 
    // 1. starting address
    // 2. adress next to last element (up to which we want sort)

    // for complete array   sort(arr,arr+n);
    // for arr upto 3rd index sort(arr,arr+4);

    // for full vector  sort(vec.begin(),vec.end());


    return 0; 
}